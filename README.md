# Real Estate Website & Design System

โปรเจกต์เว็บไซต์อสังหาริมทรัพย์ที่พัฒนาด้วย **Tailwind CSS v4** โดยมีการวางระบบ Design System ที่ชัดเจน เพื่อความสวยงามและง่ายต่อการดูแลรักษา

## 🎨 Design System

ระบบการออกแบบอ้างอิงจาก `design-system.html` โดยมีการกำหนด Tokens ต่างๆ ดังนี้:

### 1. Typography (ตัวอักษร)

ใช้ฟอนต์ **Prompt** เป็นหลักสำหรับภาษาไทยและอังกฤษ

- **Headings**: กำหนดขนาดตั้งแต่ H1 (48px) ถึง H6 (18px)
- **Body**: มี 3 ขนาด (Large, Medium, Small)
- **Price**: รูปแบบพิเศษสำหรับแสดงราคา (ExtraBold)

### 2. Color Palette (ชุดสี)

- **Primary**: `#0F172A` (Navy Blue) - ใช้สำหรับส่วนหัวและข้อความหลัก
- **Brand**: `#4CAF50` (Green) - สีหลักของแบรนด์ ใช้กับปุ่มและจุดที่ต้องการเน้น
- **Status Tags**:
  - `Featured`: สีเขียว (#4CAF50)
  - `Sale`: สีน้ำเงินเข้ม (#1E293B)
  - `Rent`: สีส้ม (#F59E0B)

### 3. Components (องค์ประกอบ)

- **Buttons**: มีหลายรูปแบบทั้ง Primary, Secondary, Outline และ Ghost
- **Property Cards**: การ์ดแสดงรายการอสังหาฯ พร้อม Custom Shape (ส่วนโค้ง) ที่มุมล่างซ้ายของรูปภาพ
- **Navbar & Footer**: ส่วนนำทางและส่วนท้ายที่รองรับ Responsive

## 📁 Project Structure

- `index.html`: หน้า Landing Page หลักของเว็บไซต์
- `design-system.html`: หน้าเอกสารรวม Components และ Tokens ทั้งหมด
- `assets/style/input.css`: ไฟล์ตั้งค่า Tailwind v4 และ Custom Styles
- `assets/style/index.css`: ไฟล์ CSS ที่ผ่านการ Compile แล้ว

## 🚀 Getting Started

1. **Install Dependencies**

   ```bash
   npm install
   ```

2. **Run Tailwind CLI (Watch Mode)**
   คำสั่งสำหรับ Compile CSS แบบ Real-time ขณะแก้ไขโค้ด

   ```bash
   npx @tailwindcss/cli -i ./assets/style/input.css -o ./assets/style/index.css --watch
   ```

3. **Open Website**
   เปิดไฟล์ `index.html` ใน Browser เพื่อดูผลลัพธ์
